# Core dependencies
torch>=2.0.0
onnx>=1.14.0

# Optional: GPU (vLLM)
# vllm>=0.4.0

# Optional: Transformers support
# transformers>=4.30.0

# Optional: NPU SDKs (별도 설치 필요)
# Rebellions: pip install rebel (Rebellions 제공)
# FuriosaAI: pip install furiosa-sdk (FuriosaAI 제공)

# Development
pytest>=7.0.0
pytest-cov>=4.0.0
black>=23.0.0
ruff>=0.1.0
mypy>=1.0.0
onnxsim>=0.4.0
