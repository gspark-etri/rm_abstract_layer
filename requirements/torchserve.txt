# TorchServe requirements
-r base.txt

# TorchServe and model archiver
torchserve>=0.8.0
torch-model-archiver>=0.8.0

# Note: TorchServe server requires Java 11+
# Install Java: sudo apt install openjdk-11-jdk

